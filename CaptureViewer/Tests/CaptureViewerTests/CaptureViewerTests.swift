import XCTest
@testable import CaptureViewer

final class CaptureViewerTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(CaptureViewer().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample)
    ]
}
